# Protean cache (protean-cache) -- ephemeral only, always reconstructable from
# Postgres (docs/INFRASTRUCTURE.md §4.3). No password in POC -- add `requirepass`
# before any non-local exposure.

bind 0.0.0.0
protected-mode no
appendonly no
save ""
maxmemory 256mb
maxmemory-policy allkeys-lru
